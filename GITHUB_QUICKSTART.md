# Быстрый старт: Публикация на GitHub

## Шаг 1: Подготовка проекта

### Windows:
Запустите файл `prepare_for_github.bat` двойным кликом

### Linux/macOS:
```bash
./prepare_for_github.sh
```

Скрипт автоматически:
- Проверит наличие секретных файлов
- Создаст/проверит `.gitignore`
- Инициализирует Git репозиторий
- Добавит файлы в Git
- Создаст первый коммит

## Шаг 2: Создание репозитория на GitHub

1. Откройте [GitHub](https://github.com) и войдите в аккаунт
2. Нажмите на `+` → `New repository`
3. Заполните форму:
   - **Repository name**: `telegram-feedback-bot` (или любое другое имя)
   - **Description**: "Telegram bot for collecting employee feedback"
   - **Public/Private**: выберите по желанию
   - **НЕ создавайте** README, .gitignore или license (они уже есть в проекте)
4. Нажмите `Create repository`

## Шаг 3: Загрузка кода на GitHub

GitHub покажет инструкции. Используйте вариант "push an existing repository":

```bash
git remote add origin https://github.com/ВАШ_USERNAME/telegram-feedback-bot.git
git branch -M main
git push -u origin main
```

Замените `ВАШ_USERNAME` на ваше имя пользователя GitHub.

## Шаг 4: Проверка

1. Обновите страницу репозитория на GitHub
2. Убедитесь, что все файлы загружены
3. **ВАЖНО:** Проверьте, что следующие файлы **НЕ появились** на GitHub:
   - `service_account.json`
   - `feedback.xlsx`
   - `bot_log.txt`

Если эти файлы случайно загрузились:

```bash
git rm --cached service_account.json
git rm --cached feedback.xlsx
git rm --cached bot_log.txt
git commit -m "Remove sensitive files"
git push
```

## Шаг 5: Настройка репозитория

### Добавьте описание:
1. Перейдите в Settings → General
2. Добавьте описание и теги (topics):
   - `telegram-bot`
   - `python`
   - `google-sheets`
   - `feedback`

### Настройте README.md:
1. Откройте файл README.md на GitHub
2. Нажмите на иконку редактирования
3. Замените `yourusername` на ваше имя пользователя
4. Сохраните изменения

## Альтернатива: Использование GitHub Desktop

1. Скачайте [GitHub Desktop](https://desktop.github.com/)
2. Установите и войдите в аккаунт
3. File → Add local repository → выберите папку проекта
4. Publish repository
5. Выберите название и приватность
6. Нажмите Publish

## Проблемы и решения

### "Permission denied (publickey)"
Настройте SSH ключ:
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
cat ~/.ssh/id_ed25519.pub
```
Добавьте публичный ключ в GitHub Settings → SSH and GPG keys

### "Authentication failed"
Используйте Personal Access Token вместо пароля:
1. GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token
3. Выберите scope: `repo`
4. Используйте токен вместо пароля при push

### Случайно загрузили секретные файлы
Удалите их из истории:
```bash
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch service_account.json" \
  --prune-empty --tag-name-filter cat -- --all

git push origin --force --all
```

**Важно:** После этого смените все ключи и токены!

## Следующие шаги

После успешной публикации:

1. Добавьте бейдж статуса в README.md
2. Создайте GitHub Actions для автоматического тестирования
3. Настройте Issues и Pull Requests templates
4. Добавьте Contributors
5. Создайте релиз (Release) с версией 1.0.0

## Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю
git log --oneline

# Создать новую ветку
git checkout -b feature/new-feature

# Обновить репозиторий
git pull

# Загрузить изменения
git add .
git commit -m "Update description"
git push

# Отменить изменения
git reset --hard HEAD

# Посмотреть удаленные репозитории
git remote -v
```

## Помощь

Если что-то пошло не так:
- [GitHub Docs](https://docs.github.com/)
- [Git Documentation](https://git-scm.com/doc)
- Создайте Issue в вашем репозитории

---

**Поздравляем! Ваш проект теперь на GitHub! 🎉**
